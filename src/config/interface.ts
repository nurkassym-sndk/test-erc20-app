export interface IConfiguration {
  envName: string;
  port: number;
  docs: string;

  chainId: number;
  rpcUrl: string;
  contractAddress: string;
  operatorAddress: string;
  operatorPk: string;
}
